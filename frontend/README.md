# Influence OS Frontend

This is the Next.js frontend for the Influence OS project.

## Getting Started
- Run `npm install` to install dependencies.
- Run `npm run dev` to start the development server.
